import Carrito from "@/components/carrito/Carrito";


const page = async () => {
  /*   const products = await getProducts(); */
  return (
    <div>
      <Carrito />
    </div>
  );
};

export default page;
